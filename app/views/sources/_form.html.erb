<%= form_with model: [ budget, source ], data: {
      source_form: true,
      controller: "form currency-fields",
      form_focused_on_toggle_value: !mobile_variant?,
      currency_fields_base_currency_value: budget.base_currency_code,
      currency_fields_reference_link_value: currency_reference_path
    } do |form| %>
  <% if source.errors.any? %>
    <section role="alert" data-form-errors>
      <h2>
        <%= t(".errors", count: source.errors.count) %>
      </h2>

      <ul>
        <% source.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </section>
  <% end %>

  <%= render "shared/name_fields", form: form, question: t(".name_question"), placeholder: t(".name_placeholder") %>
  <%= render "shared/amount_currency_fields", form: form, question: t(".amount_question"),
        amount_attribute: :amount, default_amount: true,
        currency_attribute: :currency_code, rate_attribute: :rate,
        base_currency_code: budget.base_currency_code %>

  <%= render "design_fields", form: form, source: source %>

  <div data-layout="flex">
    <%= link_to budget_sources_path(budget), role: "button", data: {
          appearance: "keycap", intent: "secondary", size: "lg",
          controller: "pressable", action: "keydown.shift+esc@document->pressable#click"
        } do %>
      <%= icon("chevron-left") %>
      <span><%= t(".cancel") %></span>
      <% unless mobile_variant? %>
        <kbd aria-hidden="true">Shift + Escape</kbd>
      <% end %>
    <% end %>
    <%= form.button type: "submit", data: {
          appearance: "keycap", intent: "primary", size: "lg",
          controller: "pressable", action: "keydown.meta+enter@document->pressable#click"
        } do %>
      <span><%= t(".submit") %></span>
      <%= icon("chevron-right") %>
      <% unless mobile_variant? %>
        <kbd aria-hidden="true">Cmd + Enter</kbd>
      <% end %>
    <% end %>
  </div>
<% end %>
