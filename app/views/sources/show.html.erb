<main data-size="sm" data-anchor="footer">
  <article data-elevation="1" style="<%= colour_styles(@source.colour) %>">
    <header>
      <nav>
        <%= link_to icon_tag("chevron-left"), budget_sources_path(@budget), aria: { label: "Back to sources" } %>
        <%= icon_tag(@source.icon) %>
        <h1><%= @source.name %></h1>
      </nav>
      <small><%= "Deleted" if @source.deleted? %></small>
    </header>

    <main>
      <dl>
        <dt>Amount</dt>
        <dd><%= formatted_amount(@source.amount, @source.currency_code) %> <%= @source.currency_code %></dd>
        <dt>Budget</dt>
        <dd><%= link_to @budget.name, budget_expenses_path(@budget) %></dd>
      </dl>
    </main>
  </article>
</main>
