<%= form_with model: budget, data: {
      budget_form: true,
      controller: "form date-fields",
      form_focused_on_toggle_value: !mobile_variant?,
      action: "change->date-fields#validate"
    } do |form| %>
  <% if budget.errors.any? %>
    <section role="alert" data-form-errors>
      <h2>
        <%= pluralize(budget.errors.count, "error") %> prevented this budget
        from being saved:
      </h2>

      <ul>
        <% budget.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </section>
  <% end %>

  <label for="base_currency_code">
    <legend>What's your main currency?</legend>
    <small>Other currencies will use this one as a reference to calculate against to.</small>

    <div>
      <%= render "shared/currency_picker", form: form, attribute: :base_currency_code,
            filter_autofocus: true %>
    </div>
  </label>

  <fieldset data-layout="flex">
    <legend>When does your budget start and end?</legend>
    <small>Mostly people select a period in 1 month between the paydays</small>

    <div>
      <%= form.date_field :starts_date, value: Date.today.beginning_of_month, required: true %>
      <%= form.date_field :ends_date, value: Date.today.end_of_month, required: true %>
    </div>
  </fieldset>

  <br>

  <%= form.button type: "submit", data: { appearance: "keycap", intent: "primary" } do %>
    Next step <%= icon("chevron-right") %>
  <% end %>
<% end %>
