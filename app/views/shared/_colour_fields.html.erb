<%# locals: (form:, open: false, input_data: {}) %>
<details name="form-details" data-form-target="section"<%= " open" if open %>>
  <summary data-form-target="summary">
    <span><%= t(".name") %>:</span>
    <small data-form-target="summaryContent"><%= form.object.colour %></small>
  </summary>

  <fieldset data-layout="flex">
    <legend class="utilities--sr-only"><%= t(".name") %></legend>
    <div>
      <%= form.collection_radio_buttons :colour, form.object.class.colour_options, :second, :first do |radio| %>
        <%= radio.label title: radio.text, style: "--form-checked-background: var(--color-palette-#{radio.value}); --form-checked-color: var(--color-invert-1);" do %>
          <%= radio.radio_button aria: { label: radio.text }, data: input_data.merge({
                form_target: "toBeFocused"
              }) { |_key, original, extra| [ original, extra ].compact.join(" ") } %>
          <%= icon("circle-dot", style: "color: var(--color-palette-#{radio.value});") %>
        <% end %>
      <% end %>
    </div>
    <% unless mobile_variant? %>
      <small>
        <%= t(".arrows_navigation_html", arrows: tag.kbd("arrow keys")) %>
      </small>
      <small>
        <%= t("shared.form_navigation.back_html", enter: tag.kbd("Enter"), shift: tag.kbd("Shift")) %>
      </small>
    <% end %>
  </fieldset>
</details>
