<article data-elevation="2" data-testid="parallel-you-lens"><main><p><%= t("lenses.parallel_you.assumption", percentage: snapshot.reduction_percentage) %></p><p><strong><%= formatted_amount(snapshot.saved, snapshot.currency_code) %> <%= snapshot.currency_code %></strong> <%= t("lenses.parallel_you.saved") %></p><small><%= t("lenses.parallel_you.alternative", amount: formatted_amount(snapshot.alternative, snapshot.currency_code), currency: snapshot.currency_code) %></small></main></article>
