<article data-elevation="2" data-testid="money-weather-lens"><main><p><strong><%= t("lenses.money_weather.conditions.#{snapshot.condition}") %></strong></p><p><%= t("lenses.money_weather.projection", days: snapshot.forecast_days, amount: formatted_amount(snapshot.projected, snapshot.currency_code), currency: snapshot.currency_code) %></p><small><%= t("lenses.money_weather.scheduled", amount: formatted_amount(snapshot.scheduled, snapshot.currency_code), currency: snapshot.currency_code) %></small></main></article>
